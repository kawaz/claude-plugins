# バージョン更新ルール

## 更新が必要な場合

各プラグインで**動作に関わるファイル**の更新があった場合：

- hooks/ 内のスクリプト変更
- hooks.json の変更
- skills/ 内のファイル変更
- plugin.json の設定変更（バージョン以外）

## 更新が不要な場合

**動作に影響しない**変更のみの場合：

- README.md の更新
- CLAUDE.md の更新
- コメントのみの変更
